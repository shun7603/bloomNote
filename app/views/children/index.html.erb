<h2>登録済みの子ども一覧</h2>

<% @children.each do |child| %>
  <div>
    <strong>名前:</strong> <%= child.name %><br>
    <strong>生年月日:</strong> <%= child.birth_date.strftime("%Y年%m月%d日") %><br>
    <strong>年齢:</strong> <%= ((Date.today - child.birth_date).to_i / 365.25).floor %>歳
  </div>
  <hr>
<% end %>